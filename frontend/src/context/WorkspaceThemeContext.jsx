import { useEffect, useMemo, useState } from "react";import { fetchPublicSite } from "../services/api.js";import WorkspaceThemeContext from "./WorkspaceThemeContextValue.js";
export function WorkspaceThemeProvider({children}){const[site,setSite]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{const timer=window.setTimeout(()=>fetchPublicSite().then(setSite).catch(()=>setSite(null)).finally(()=>setLoading(false)),0);return()=>window.clearTimeout(timer)},[]);useEffect(()=>{const b=site?.branding;if(!b)return;const root=document.documentElement;root.style.setProperty("--workspace-primary",b.primaryColor);root.style.setProperty("--workspace-accent",b.accentColor);root.style.setProperty("--workspace-background",b.surfaceMode==="light"?"#f7f8f7":"#090b0a");root.style.setProperty("--workspace-surface",b.surfaceMode==="light"?"#ffffff":"#151816");root.style.setProperty("--workspace-text",b.surfaceMode==="light"?"#111512":"#f7f8f7");root.style.setProperty("--workspace-muted",b.surfaceMode==="light"?"#667069":"#a5afa8");root.style.setProperty("--workspace-border",b.surfaceMode==="light"?"#dce2de":"#2a302c");if(b.faviconUrl){let link=document.querySelector("link[rel='icon']");if(link)link.href=b.faviconUrl}},[site]);const value=useMemo(()=>({site,loading}),[site,loading]);return <WorkspaceThemeContext.Provider value={value}>{children}</WorkspaceThemeContext.Provider>}

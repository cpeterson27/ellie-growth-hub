import { useCallback, useEffect, useMemo, useState } from "react";
import { useSearchParams } from "react-router-dom";
import { FiEye, FiMail, FiRefreshCw } from "react-icons/fi";
import Button from "../components/Button.jsx";
import DashboardCard from "../components/DashboardCard.jsx";
import Modal from "../components/Modal.jsx";
import { fetchCampaigns, fetchOutreach, generateOutreach, sendEmails, updateOutreach } from "../services/api.js";
import "./Outreach.css";

const labels={pending:"Pending review",approved:"Approved",sent:"Sent",replied:"Replied",failed:"Failed"};
export default function Outreach(){const [params]=useSearchParams();const [campaigns,setCampaigns]=useState([]);const [selected,setSelected]=useState(null);const [items,setItems]=useState([]);const [filter,setFilter]=useState("all");const [search,setSearch]=useState("");const [preview,setPreview]=useState(null);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [error,setError]=useState("");const loadItems=useCallback(async campaign=>{if(!campaign?._id){setItems([]);return;}const data=await fetchOutreach(campaign._id);setItems(Array.isArray(data)?data:data.outreach||[])},[]);const load=useCallback(async()=>{try{setLoading(true);const data=(await fetchCampaigns()).filter(Boolean);setCampaigns(data);const campaign=data.find(x=>x._id===params.get("campaignId"))||data[0]||null;setSelected(campaign);await loadItems(campaign)}catch{setError("Unable to load outreach.")}finally{setLoading(false)}},[params,loadItems]);useEffect(()=>{load()},[load]);const filtered=useMemo(()=>items.filter(item=>(filter==="all"||item.status===filter)&&(!search||[item.organization,item.contactName,item.contactEmail,item.subject].filter(Boolean).join(" ").toLowerCase().includes(search.toLowerCase()))),[items,filter,search]);const counts=Object.fromEntries(["pending","approved","sent","replied","failed"].map(s=>[s,items.filter(x=>x.status===s).length]));const generate=async()=>{if(!selected)return setError("Select a campaign first.");try{setSaving(true);setError("");await generateOutreach(selected._id);await loadItems(selected)}catch(err){setError(err.response?.data?.error||"Unable to generate outreach.")}finally{setSaving(false)}};const approve=async item=>{try{setSaving(true);const updated=await updateOutreach(item._id,{status:"approved"});setItems(current=>current.map(row=>row._id===updated._id?updated:row))}catch{setError("Unable to approve outreach.")}finally{setSaving(false)}};const send=async()=>{const ids=items.filter(x=>x.status==="approved").map(x=>x._id);if(!ids.length)return setError("Approve one or more drafts before sending.");try{setSaving(true);setError("");await sendEmails(ids);await loadItems(selected)}catch(err){setError(err.response?.data?.error||"Unable to send approved emails.")}finally{setSaving(false)}};return <div className="page-dashboard outreach-page"><header className="outreach-header"><div><p className="outreach-eyebrow">Campaign delivery</p><h1 className="page-title">Outreach</h1><p>Generate drafts, review each message, approve, then send only the approved emails.</p></div><div className="outreach-header__actions"><Button variant="outline" loading={saving} onClick={generate}><FiRefreshCw/>Generate drafts</Button><Button loading={saving} onClick={send}><FiMail/>Send approved ({counts.approved||0})</Button></div></header>{error?<p className="form-error">{error}</p>:null}<section className="outreach-controls"><label>Campaign<select className="select-input" value={selected?._id||""} onChange={async e=>{const next=campaigns.find(c=>c._id===e.target.value)||null;setSelected(next);await loadItems(next)}}>{campaigns.map(c=><option key={c._id} value={c._id}>{c.name}</option>)}</select></label><input className="select-input" placeholder="Search contacts, companies, or subjects" value={search} onChange={e=>setSearch(e.target.value)}/></section><section className="outreach-summary">{["pending","approved","sent","replied","failed"].map(status=><button key={status} className={filter===status?"is-active":""} onClick={()=>setFilter(filter===status?"all":status)}><span>{labels[status]}</span><strong>{counts[status]||0}</strong></button>)}</section><DashboardCard title={selected?`Messages for ${selected.name}`:"Outreach messages"}>{loading?<p>Loading outreach…</p>:filtered.length?<div className="outreach-list">{filtered.map(item=><article key={item._id} className="outreach-item"><div className="outreach-item__top"><div><p className="outreach-item__company">{item.organization||"Independent contact"}</p><h3>{item.contactName||item.contactEmail||"Contact"}</h3><p>{item.contactEmail||"No email"}</p></div><span className={`outreach-status outreach-status--${item.status}`}>{labels[item.status]||item.status}</span></div><p className="outreach-item__subject">{item.subject||"No subject"}</p><div className="outreach-item__actions"><Button variant="outline" size="sm" onClick={()=>setPreview(item)}><FiEye/>Review</Button>{item.status==="pending"?<Button size="sm" loading={saving} onClick={()=>approve(item)}>Approve</Button>:null}{item.status==="failed"&&item.errorMessage?<span className="outreach-item__error">Delivery error recorded</span>:null}</div></article>)}</div>:<div className="table-state table-state--empty">No outreach items match this view. Generate drafts only after the campaign contacts are ready.</div>}</DashboardCard><Modal isOpen={Boolean(preview)} onClose={()=>setPreview(null)} title="Review outreach email" footer={preview?.status==="pending"?<Button onClick={()=>{approve(preview);setPreview(null)}}>Approve draft</Button>:<Button variant="outline" onClick={()=>setPreview(null)}>Close</Button>}>{preview?<div className="outreach-preview"><p><strong>To</strong> {preview.contactName||"Contact"} {preview.contactEmail?`<${preview.contactEmail}>`:""}</p><p><strong>Subject</strong> {preview.subject||"No subject"}</p><div>{preview.body||preview.message||"No email body has been stored."}</div></div>:null}</Modal></div>}

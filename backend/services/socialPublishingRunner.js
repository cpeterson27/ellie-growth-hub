const service=require("./socialPublishingService");let timer=null,running=false;async function runDueSocialPublishing(){if(running)return[];running=true;try{return await service.runDue()}finally{running=false}}function startSocialPublishingRunner({force=false}={}){if(timer||(!force&&process.env.RUN_SOCIAL_PUBLISHING_IN_PROCESS!=="true"))return timer;timer=setInterval(()=>runDueSocialPublishing().catch(error=>console.error("Social publishing runner failed:",error.message)),30000);timer.unref?.();return timer}module.exports={runDueSocialPublishing,startSocialPublishingRunner};
